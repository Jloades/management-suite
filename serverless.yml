service: management-suite

frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs12.x

functions:
  getAccount:
    handler: api\handlers\http\users\user\get.handler
    events:
      - http:
          path: /users/{userId}
          method: get
plugins:
  - serverless-offline



